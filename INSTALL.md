# Инструкция по установке и запуску

## 🚀 Быстрый старт

**Самый простой способ:**

1. Установите Node.js через официальный установщик: https://nodejs.org/ (скачайте LTS версию)
2. Перезапустите терминал
3. Выполните в терминале:
   ```bash
   cd "/Users/zakharkorchagin/My project/relationship-app"
   ./setup.sh
   ```

Или вручную:
```bash
cd "/Users/zakharkorchagin/My project/relationship-app"
npm install
npm run dev
```

---

## Шаг 1: Установка Node.js

Для работы приложения необходимо установить Node.js (версия 18 или выше).

### ⭐ Вариант 1: Официальный установщик (САМЫЙ ПРОСТОЙ)

1. Перейдите на сайт [nodejs.org](https://nodejs.org/)
2. Скачайте **LTS версию** для macOS (зеленая кнопка)
3. Запустите скачанный `.pkg` файл и следуйте инструкциям установщика
4. **Важно:** После установки перезапустите терминал (закройте и откройте заново)
5. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

### Вариант 2: Установка через Homebrew

Если у вас установлен Homebrew:

```bash
brew install node
```

Проверьте установку:
```bash
node --version
npm --version
```

Если у вас проблемы с правами доступа, сначала выполните:
```bash
sudo chown -R $(whoami) /opt/homebrew
```

Затем:
```bash
brew install node
```

### Вариант 3: Установка через nvm (Node Version Manager)

```bash
# Установите nvm (если еще не установлен)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезапустите терминал или выполните:
source ~/.zshrc

# Установите Node.js
nvm install --lts
nvm use --lts
```

## Шаг 2: Установка зависимостей проекта

После установки Node.js перейдите в директорию проекта:

```bash
cd "/Users/zakharkorchagin/My project/relationship-app"
```

Установите зависимости:

```bash
npm install
```

Это может занять несколько минут.

## Шаг 3: Запуск приложения

Запустите сервер разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

Откройте этот адрес в браузере.

## Дополнительные команды

- `npm run build` - сборка для продакшена
- `npm run start` - запуск продакшен версии
- `npm run lint` - проверка кода линтером

## Решение проблем

### Ошибка "command not found: npm"
- Убедитесь, что Node.js установлен: `node --version`
- Перезапустите терминал после установки Node.js
- Проверьте PATH: `echo $PATH`

### Ошибки при установке зависимостей
- Убедитесь, что у вас есть интернет-соединение
- Попробуйте очистить кеш: `npm cache clean --force`
- Удалите `node_modules` и `package-lock.json`, затем запустите `npm install` снова

### Порт 3000 занят
- Используйте другой порт: `npm run dev -- -p 3001`
- Или остановите процесс, использующий порт 3000
